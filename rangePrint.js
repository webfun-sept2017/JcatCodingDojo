
// Printing printRange(2,10,2)

function rangePnt(numb1, numb2) {
    var a = "";
        for (var i = numb1; i < numb2; i++) {
            a += i + ',';
        }
    return a;

}

rangePnt(2,10,2);
